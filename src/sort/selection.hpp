#ifndef SELECTION_HPP
#define SELECTION_HPP
#include <vector>
/**
 * Sorts a given array of ints in place using the selection sort algorithm.
 *
 * Selection sort works by repeatedly going through each index of the array,
 * moving the smallest element to the first unmodified index.
 */
void selection(std::vector<int> &numbers);
#endif
